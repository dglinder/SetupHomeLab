$TTL    604800
@       IN      SOA     okd4-services.okd.local. admin.okd.local. (
                  6     ; Serial
             604800     ; Refresh
              86400     ; Retry
            2419200     ; Expire
             604800     ; Negative Cache TTL
)

; name servers - NS records
    IN      NS      okd4-services.okd.local.

; name servers - PTR records
{{ hostvars["okd4-services"].ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4-services.okd.local.

; OpenShift Container Platform Cluster - PTR records
{{ hostvars["okd4-bootstrap"].ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4-bootstrap.lab.okd.local.
{{ hostvars["okd4-control-plane-0"].ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4-control-plane-0.lab.okd.local.
{{ hostvars["okd4-control-plane-1"].ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4-control-plane-1.lab.okd.local.
{{ hostvars["okd4-control-plane-2"].ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4-control-plane-2.lab.okd.local.
{{ hostvars["okd4-compute-0"].ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4-compute-0.lab.okd.local.
{{ hostvars["okd4-compute-1"].ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4-compute-1.lab.okd.local.
{{ hostvars["okd4-services"].ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    api.lab.okd.local.
{{ hostvars["okd4-services"].ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    api-int.lab.okd.local.
