$TTL    604800
@       IN      SOA     okd4-services.okd.local. admin.okd.local. (
                  6     ; Serial
             604800     ; Refresh
              86400     ; Retry
            2419200     ; Expire
             604800     ; Negative Cache TTL
)

; name servers - NS records
    IN      NS      okd4_services.okd.local.

; name servers - PTR records
{{ hostvars.okd4_services.ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4_services.okd.local.

; OpenShift Container Platform Cluster - PTR records
{{ hostvars.okd4_bootstrap.ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4_bootstrap.lab.okd.local.
{{ hostvars.okd4_control_plane_0.ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4_control_plane_0.lab.okd.local.
{{ hostvars.okd4_control_plane_1.ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4_control_plane_1.lab.okd.local.
{{ hostvars.okd4_control_plane_2.ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4_control_plane_2.lab.okd.local.
{{ hostvars.okd4_compute_0.ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4_compute_0.lab.okd.local.
{{ hostvars.okd4_compute_1.ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    okd4_compute_1.lab.okd.local.
{{ hostvars.okd4_services.ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    api.lab.okd.local.
{{ hostvars.okd4_services.ansible_default_ipv4.address.split('.')[-1] }}    IN    PTR    api-int.lab.okd.local.
